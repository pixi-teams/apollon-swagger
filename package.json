{
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2",
    "prettier-plugin-openapi": "^1.0.13"
  },
  "lint-staged": {
    "*.{yaml,yml,json}": "prettier --write"
  }
}
